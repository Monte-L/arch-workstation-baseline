# VPN and DNS Baseline

## Purpose

This document records the current VPN and DNS behavior of the workstation.

Raw VPN, DNS, and routing outputs are stored locally under private/raw/ and must not be published.

## VPN Provider

| Item | Value |
|---|---|
| VPN provider | ProtonVPN |
| VPN technology | WireGuard |
| Active VPN interface | protonch |

## VPN State During Active Test

During the active VPN test, the ProtonVPN WireGuard interface was detected and traffic was observed through the tunnel.

| Item | Status |
|---|---|
| VPN interface detected | Yes |
| Interface name | protonch |
| Handshake observed | Yes |
| Data transfer observed | Yes |
| Allowed IPs | 0.0.0.0/0 |
| Policy routing detected | Yes |
| VPN routing table | 51820 |
| Public IPv4 route decision | Via protonch |

## Routing Behavior

The main routing table still shows the Wi-Fi interface as the default route.

However, policy routing rules are present, and the route decision for a public IPv4 address selected the VPN interface.

Observed sanitized route decision:

- dev protonch
- table 51820

This indicates that the VPN is active and that public traffic is being routed through the ProtonVPN WireGuard interface using policy routing.

## DNS State

DNS is managed by NetworkManager through /etc/resolv.conf.

A direct check against /etc/resolv.conf did not detect Quad9 public resolver addresses.

| Item | Status |
|---|---|
| DNS manager | NetworkManager |
| systemd-resolved | Disabled / inactive |
| /etc/resolv.conf | Generated by NetworkManager |
| Quad9 direct detection | Not detected directly |
| DNS validation status | Requires further testing |

## Current Assessment

VPN routing appears to be working correctly during the active VPN test.

The workstation uses ProtonVPN through a WireGuard interface named protonch.

Although the main routing table shows Wi-Fi as the default route, policy routing sends public IPv4 traffic through the VPN interface.

DNS behavior still requires additional validation because Quad9 was not detected directly in /etc/resolv.conf.

## Required Follow-Up Tests

1. Verify external IP before and after VPN activation.
2. Verify DNS resolver behavior while VPN is active.
3. Check for DNS leaks.
4. Confirm whether DNS is handled by ProtonVPN, Quad9, or NetworkManager.
5. Decide whether Quad9 should be enforced outside the VPN.
6. Document expected behavior for VPN-on and VPN-off states.

## Sensitive Information Excluded

The following information must not be published:

- WireGuard private keys
- WireGuard peer public keys
- VPN endpoints
- public IP addresses
- DNS resolver IPs, unless intentionally documented
- local gateway IP address
- full routing table
- full WireGuard configuration
- raw VPN status output

## VPN Kill Switch Status

At the current baseline stage, the VPN works when active, and public IPv4 routing was confirmed through the ProtonVPN WireGuard interface.

However, the system does not yet enforce VPN-only outbound traffic during boot.

Current outbound firewall policy allows outgoing traffic by default. This means the workstation may be able to reach the internet through the regular Wi-Fi connection before the VPN interface is active.

This is not a failure of the current VPN setup. It is a separate hardening layer that has not yet been implemented.

A future VPN kill switch should enforce:

1. No normal outbound traffic before VPN activation.
2. Outbound traffic allowed only to the VPN endpoint before tunnel establishment.
3. Normal internet traffic allowed only through the VPN interface.
4. Safe rollback commands in case connectivity breaks.
5. Separate testing for VPN-on and VPN-off states.

Status: open hardening item.

## DNS Behavior Review - VPN Active

A VPN-active DNS behavior review was performed.

Findings:

| Item | Result |
|---|---|
| VPN interface | protonch |
| Public route decision | via protonch |
| VPN routing table | 51820 |
| systemd-resolved | disabled / inactive |
| resolv.conf generator | resolvconf |
| Quad9 direct detection | detected |

This improves the earlier DNS baseline.

Earlier checks did not detect Quad9 directly in /etc/resolv.conf. During the VPN-active test, Quad9 was detected directly.

Current interpretation:

- VPN routing works while ProtonVPN is active.
- Quad9 DNS is visible in the active resolver configuration while VPN is active.
- DNS leak protection still requires external validation.
- VPN-off DNS behavior still needs to be reviewed.
